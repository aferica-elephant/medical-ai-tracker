server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medical_ai_tracker?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai
    username: root
    password: sdy10010
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data.sql
      encoding: UTF-8

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# AgentScope 智能体框架配置（OpenAI 兼容格式，对接火山引擎 Ark API）
agentscope:
  model:
    provider: openai
  openai:
    enabled: true
    api-key: 04ac97b3-51c1-4060-ba15-f8cd6ae25b5b
    model-name: deepseek-v4-flash-260425
    base-url: https://ark.cn-beijing.volces.com/api/v3
    stream: false
  dashscope:
    enabled: false
  gemini:
    enabled: false
  anthropic:
    enabled: false

search:
  cron: "0 0 2 ? * MON"
